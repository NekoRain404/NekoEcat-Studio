# src/core — Shared Types & Protocol

Domain types and the JSON framing protocol shared between GUI and daemon.

## Files

| File | Purpose |
|------|---------|
| `EthercatTypes` | `SlaveInfo`, master state, port info, SDO result |
| `JsonProtocol` | Newline-delimited JSON request/response framing |
